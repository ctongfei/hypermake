def Exec(interpreter="python") <- (input="script.py"):
  $interpreter $input

@Exec(interpreter="python")
task run(x="test") -> ():
  import os
  globals().update(os.environ)
  print(x)

plan Run = {run}
