local_root = "/home/tongfei/proj/forge/target"

task t0(x = "1") -> out:
  sleep 5
  echo "0" > $out

task t1(x = $t0.out) -> out:
  sleep 2
  cat $x > $out

task t2(x = $t1.out) -> out:
  sleep 2
  cat $x > $out


plan Run = { t2 }
