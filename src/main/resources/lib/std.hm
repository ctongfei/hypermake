## Links the given path as a package.
def symlink(path) -> out:
  ln -s $path $out

## Execute the script with the given interpreter.
class run(interpreter):
  def run(_interal_interpreter_script):
    $interpreter $_interal_interpreter_script
